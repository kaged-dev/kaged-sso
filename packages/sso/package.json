{
	"name": "@kaged/sso",
	"version": "0.0.1",
	"description": "Stateless kaged SSO relay: performs the OAuth dance with upstream providers and mints short-lived ES256 identity tokens. Self-hostable, no database.",
	"license": "AGPL-3.0",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kaged-dev/kaged-sso.git"
	},
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"files": [
		"src"
	],
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"start": "bun run src/main.ts",
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"jose": "^5.9.6"
	}
}
