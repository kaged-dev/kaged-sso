{
	"name": "kaged-sso",
	"private": true,
	"type": "module",
	"version": "0.0.1",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"test": "bun test --recursive packages/",
		"typecheck": "bun --filter '*' typecheck",
		"fixtures:generate": "bun run packages/sso-fixtures/scripts/generate.ts",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@types/bun": "^1.3.0",
		"typescript": "^5.8.0"
	},
	"dependencies": {
		"jose": "^5.9.6"
	}
}
